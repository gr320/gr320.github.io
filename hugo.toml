# ========================================
# Hugo 站点配置
# ========================================
# 
# 注意：此文件已被简化
# 主要配置位于 config/_default/ 目录下
# 
# 配置文件优先级：
# 1. config/{environment}/  （如 config/production/）
# 2. config/_default/       （默认配置，会覆盖此文件）
# 3. hugo.toml              （根目录配置，优先级最低）
#
# 建议：所有配置都在 config/_default/ 目录中管理
# ========================================

# 如果需要覆盖特定配置，可以在这里添加
# 例如：
# baseURL = "https://yourdomain.com"

